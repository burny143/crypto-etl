# Project Status

*Last updated: 2026-08-20*

## Overall State

**Phase:** 3 — Research-to-Chart Traceability (documentation & preparation) + Kronos research overlay (new)

## Working Features

### Data Pipeline
- ✅ Historical OHLCV ETL: CCXT → Supabase `crypto_historical` (227,200 rows, 30 symbols × 3 timeframes)
- ✅ Price snapshot ETL: GitHub Actions every 30 min → Supabase `crypto_data`
- ✅ Strategy research: 8 strategies, 9,437 variants, walk-forward validated
- ✅ GitHub Actions CI/CD for all pipelines
- ✅ **Kronos + Trend signal ETL** (`kronos_signal_etl.py` + workflow `kronos_signals.yml`): scheduled walk-forward Kronos predictions → `kronos_predictions` / `kronos_signals` (research overlay, no-lookahead, honest metrics per series)

### Frontend (`index.html`)
- ✅ Landing page → Launch Terminal flow
- ✅ Candlestick chart (Lightweight Charts v5.2.0)
- ✅ 12+ technical indicators (client-side JS, max 3 visible)
- ✅ Symbol selector (30 pairs) + timeframe selector (1h/4h/1d)
- ✅ Signal engine (user-defined conditions → chart BUY/SELL markers)
- ✅ AI-style research panel
- ✅ Paper trading panel (order form, positions, live P&L, order history, $10K cash)
- ✅ Top Strategies panel (loads strategy results from Supabase)

### Frontend (`charts.html`)
- ✅ **Kronos AI overlay**: toggle loads precomputed `sma50` + `predicted_close` overlays and buy/sell markers from Supabase, and surfaces walk-forward honesty metrics (dir-acc, net vs buy&hold, evaluated range). Research overlay — no model in the browser (ADR-0007)

### Database (Supabase)
- ✅ `crypto_historical` — OHLCV bars
- ✅ `crypto_data` — Current prices
- ✅ `crypto_research` — AI research entries
- ✅ `paper_orders` / `paper_positions` / `paper_equity_curve` — Paper trading
- ✅ `strategy_results` / `research_runs` — Strategy research
- ✅ `kronos_predictions` / `kronos_signals` — Kronos + Trend research overlay (V9 migration, RLS anon-read/service-write)
- ✅ RLS policies for anon key access on all client-facing tables

## Known Issues

| Issue | Status | Notes |
|-------|--------|-------|
| Paper trading cash resets on page reload | Unresolved | No persistence to equity curve |
| Strategy results → chart markers traceability gap | Unresolved | No signal_id linking research to chart markers |
| Duplicated strategy/indicator logic in Python vs JS | Documented | Not changed — intentional for independence |
| Frontend (React) abandoned | Permanent | All features ported to index.html |
| Backend (FastAPI) abandoned | Permanent | All logic ported to client-side JS |
| No paginated data loading | Unresolved | All bars loaded at once |
| No walk-forward validation in UI | Unresolved | Currently only shows OOS/IS labels |
| Simulation realism mode pins price at clamps | Open | GARCH+t(3) tails hit 10× floor/ceiling ~1.5–3% of ticks, letting strategies "arbitrage" the clamps — realism-mode backtest alpha (+9000%) was inflated by that + by compounding position sizing. The 5-step refactor removed the sizing side (see Recent Changes 2026-07-31): alpha collapsed from +8912%/+2369% (mean/median) to +444%/+411% with mean/median ratio ~1.1× (no compounding snowball). The remaining edge is plausibly genuine regime exploitation; the price-clamp arb itself is still open |

## Current Milestone

**Research-to-Chart Vertical Slice** — see `.ai/current-milestone.md` for details.

Status: Preparing documentation and data contracts. No runtime code changes in this phase.

## Recent Changes

- **2026-08-20**: Kronos + Trend research overlay fixed and live. **Root cause:** `kronos_signal_etl.py` passed `x_timestamp=x.index` (a `DatetimeIndex`), but the vendored `KronosPredictor.predict` → `calc_time_stamps()` requires a `pandas.Series` (uses the `.dt` accessor). Every walk-forward bar threw `AttributeError` and was swallowed as `[WARN] prediction failed` — workflow exit 0, **zero rows**. Fix (`a01539d`): pass `pd.Series(x.index.values)` / `pd.Series([...])` as timestamps; `load_ohlcv` also drops NaN-volume bars (predictor derives `amount` from volume). Verified locally on real BTC-USDT 1d (600 bars, 5/5 predictions OK, ~0.1s/bar CPU) + workflow run #2 (`32367391147`) success: **14,220 rows each** in `kronos_predictions` / `kronos_signals` (30 symbols × 1d/4h/1h, 158 eval bars), sane prices/votes, buy/sell/flat signals with honesty metrics. Tests: `tests/test_kronos_signal.py` 13/13 pass. Docs: `docs/data-contracts/kronos-signal.md`, `ADR-0007`. Honesty contract preserved: raw Kronos direction has NO validated edge (~40–49% sign accuracy), metrics compare predicted-vs-last-actual-close, ties stay 0.
- **2026-07-31**: Bug fixes on `js/contracts.js` (15 items across 4 classes). **CRITICAL** (2): `BacktestTrade.calculatePnL()` now guards against `quantity === 0 || entryPrice === 0` before dividing (was silently producing NaN/Infinity); `PerformanceMetrics.getVolatilityAdjustedReturn()` now uses `Math.abs(this.maxDrawdownPct)` for the divisor magnitude and replaces the `|| 0.001` falsy-coercion fallback with an explicit zero check (`absDrawdown === 0 ? 0.001 : absDrawdown`), fixing wrong-sign results when maxDrawdownPct is stored as a negative number (e.g. -15). **HIGH** (7): `ExecutableSignal` constructor — dead/contradictory if-block (`_executed = false` forced even when execution data was supplied) replaced with correct `_executed = true` when both `executionPrice !== undefined && executionFee !== undefined`; constructor parameter order reordered to mirror `Signal` (signalId is now 8th argument after metadata, not last; `createdAt = Date.now()` added and forwarded to `super()`); `Signal.getChartColor()`/`getChartShape()` now throw a descriptive error for unrecognized directions instead of silently defaulting to SELL styling; `PerformanceMetrics.isStatisticallySignificant(confidence)` now derives a confidence-appropriate z-score threshold (90%→1.28, 95%→1.645, 99%→2.33, default 1.645) instead of ignoring the parameter and using a hardcoded 1.0; `ResearchResult.formatSummary()` now uses optional chaining (`?.`) with nullish coalescing (`?? 'N/A'`) for sharpeRatio so missing/malformed data doesn't throw; `BacktestTrade` constructor now throws if `exitTime <= entryTime`; fee-unit mismatch (`BacktestTrade.fees` = percentage per leg vs `ExecutableSignal.executionFee` = flat currency) is documented via inline comments on both `calculatePnL()` and `calculateExecutionPnL()`. **MEDIUM/LOW** (5): `calculatePositionSize()` throws when `riskAmount === 0` (zero portfolio or risk percent); type guards added to `formatForChart()`, `toChartMarker()`, and `formatForDisplay()` before calling `.toLocaleString()`/`.substring()` on potentially-undefined fields; `Math.floor`→`Math.round` in `toChartMarker()` for accurate ms→s conversion; `PnLBreakdown.validateCalculation()` expanded to also verify fee breakdown consistency (`entryFee + exitFee ≈ totalFees`) and slippage cost consistency (`slippage * quantity ≈ slippageCost`); export section refactored to iterate over the `api` object instead of duplicating class names. All exports preserved (Signal, SignalMetadata, ConfidenceFactors, MarketSnapshot, BacktestTrade, PnLBreakdown, ResearchResult, PerformanceMetrics, ExecutableSignal). Syntax validated.
- **2026-07-31**: Trade-summary popup on the simulation terminal (`js/simulation.js`, `simulation.html`) — clicking **Stop** now opens a modal summarizing the just-finished run: equity, return %, trade counts, win rate, net PnL, fees paid, open positions + unrealized PnL, and a per-strategy breakdown (entries/exits/closed/wins/win rate/PnL, sorted by PnL). Numbers come from new `Portfolio` running counters (`totalRealizedPnl`, `strategyStats` keyed by `strategyId`) rather than the UI-capped 100-entry trades array, so they stay accurate past 100 trades; `openPosition()`/`closePosition()` maintain the counters (close PnL net of entry+exit fees), `reset()` clears them, and `_buildTradeSummary()` is pure data (no DOM) so headless/batch paths are untouched. Modal closes via ✕ button, backdrop click, or Escape (keydown listener removed on close); `showTradeSummary()` is guarded by `headless`/`document` so batch runs never touch the DOM. **Two minor UI-only fixes applied afterward:** (1) **Singleton modal** — `showTradeSummary()` now removes any existing `.summary-overlay` and its `keydown` listener before creating a new modal, preventing DOM stacking when Stop is pressed multiple times without closing the prior modal. `this._summaryKeyHandler` tracks the current listener so it can be reliably cleaned. The **Start** button is also disabled while the modal is open (re-enabled on close) so a user cannot launch a new run while viewing a stale summary. (2) **Exit-row strategy attribution** — in `_processSignal()`'s `exit_long` branch, the trade row is now labeled with `result.strategyId` (the entry strategy from `closePosition()`) instead of `sig.strategy` (the exit-signal strategy). `closePosition()` returns `{..., strategyId: pos.strategyId}` so both the trades table and the by-strategy summary consistently attribute the trade to the strategy that opened (and bore the risk of) the position. This only matters in multi-strategy ("all") mode; single-strategy and headless/batch runs are unaffected since `runHeadless()` always sets `activeStrategies = [strategy]`. Validation: singleton fix confirmed — only one overlay in DOM at any time, keydown listener not duplicated, Start disabled while open, re-enabled on close; exit attribution verified — `closePosition()` returns strategyId matching entry strategy; batch regression + headless determinism intact. Backup: `.backups/js-simulation.js.20260731-130512.bak`, `.backups/simulation.html.20260731-130512.bak`
- **2026-07-31**: Critical bug-fix on the simulation terminal (`js/simulation.js`) — **`_pendingSignals` state leaked across sequential `runHeadless()` calls.** `runBatchBacktest()` reuses ONE engine across all 150 runs, and strategy evaluation fires every 5 ticks with ticks=20000 divisible by 5, so the final tick of a run could queue a signal with no next tick to fill it — and since `runHeadless()` reset market/portfolio/counters but NOT `_pendingSignals`, that stale signal survived into the next run, where `_fillPendingSignals()` filled it on tick 1 against the new seed's market: a phantom trade (fees, PnL, winRate, returnPct) injected into a run it had nothing to do with, making results depend on call order instead of seed. Demonstrated on the pre-fix backup: a leftover `buy_and_hold` signal turned a clean $10,033.46 / 38-trade run into $10,082.49 / 40 trades. **Fix:** `runHeadless()` now clears `_pendingSignals` in its per-run reset block (same as `reset()` does for the live path) AND discards/counts any signal still pending at run end (`unfilledSignals` in the return object). Batch impact: 22/100 strategy runs ended with a queued signal — the previously reported 50-seed batch was contaminated (breakout mean α 444.14% → 441.41%, RSI p10 131.45% → 120.46%). Also fixed the LOW issue: hardcoded `10000` literals in `_render()` (totalPnl/retPct) and `runHeadless()` (peakEquity/returnPct) now derive from `this.portfolio.initialCash`. Validation: 15/15 regression checks — forced final-tick leak (pre-fix: results differ / post-fix: identical to fresh engine), shared-engine==fresh-engine at 20000 ticks across 3 strategies × 2 realism, two successive full batches bit-identical, determinism preserved, stubless require intact. Backup: `.backups/js-simulation.js.20260731-122415.bak`
- **2026-07-31**: 5-step realism refactor on the simulation terminal (`js/simulation.js`, `js/backtest.js`). **1) Bounded non-compounding sizing:** `_processSignal` now sizes off `initialCash` (5–15% by confidence) instead of growing cash, with a hard `maxPositionPct = 0.15` cap on fill notional vs current equity — new shared `Portfolio.computeEntrySize()`; the cap is floored (not rounded) so `qty × fillPrice ≤ 15% × equity` holds exactly for every fill (clamp-audited: 225 entries, 0 violations). **2) One-tick execution lag:** signals evaluated on candle N are queued (`_pendingSignals`, one per strategy) and filled on candle N+1 at its OPEN, shared by live `_step()` and headless runs; buy-and-hold baseline fills one tick later at next open too (live `_refreshBaseline` mirrors the fill math incl. volume-aware slippage, staying bit-identical to headless B&H). **3) Volume-aware slippage:** `effectiveSlippageBps = base × (1 + k × orderNotional/(volume×price))` with `k = 50`, flat-bps fallback when volume is absent; candle volume threaded through entry/exit fills. **4) Risk-adjusted metrics:** `runHeadless` returns per-tick Sortino (0% target, downside deviation); batch report adds a Sortino column and a prominent CVaR-style p10 tail-risk section. **5) Re-run (50 seeds × 20k ticks, realism ON + randomize ON):** breakout mean α +8912% → +444% (median +2369% → +411%), RSI mean α +3221% → +290% (median +2597% → +347%); mean/median ratio dropped ~3.8× → ~1.1×, p10 (worst decile) +117%/+131%, beats B&H 96%/90%, median maxDD 35.9%/18.2%. Legacy mode now shows the honest economics: breakout −50% / RSI −29% median α vs B&H (active trading loses to buy-and-hold on a plain GBM net of costs). Validation: 24/24 regression checks — stubless require, 18/18 headless==live parity, live-baseline==headless-B&H, execution-lag fill-at-next-open (121 fills), clamp bounds, prior-fix regressions (falsy-zero, amortized trim, counters, initialCash reset, TRADING_COSTS, buy_and_hold filter), seeded determinism. 5.6s → 4.6s batch. Backups: `.backups/js-simulation.js.20260731-115737.bak`, `.backups/js-backtest.js.20260731-115737.bak`
- **2026-07-31**: Second bug-fix batch (10 items) on the simulation terminal. **Super critical:** all browser-only top-level code in `simulation.js` (visibility handler, chart creation, control wiring, ResizeObserver) is now guarded by `if (typeof document !== 'undefined' && document.getElementById)` — `require('./backtest.js')` works under plain Node with zero browser stubs (verified via stubless child-process probe), so the headless CI batch workflow actually functions. **Critical:** falsy-zero option resolution in `SyntheticMarket` (alpha/minSigma/volumeCoupling.strength use `!== undefined` checks now, so explicit `0` is honored — volume coupling can truly be disabled); candle-buffer trim is amortized (trim only when length > maxBuffer×1.1) turning a per-tick O(n) full-array copy into O(1) average — 50-seed × 20k-tick batch dropped 30.1s → 5.6s; winRate now computed from running `closedTradesCount`/`winningTradesCount` counters instead of filtering the UI-capped 100-entry trades array (a 229-close run previously reported winRate over only the last 100 trades). **High:** `Portfolio.reset()` restores `initialCash` not hardcoded 10000; `chkRealism` toggle now calls `engine.market.reset()` in both directions so the chart genuinely regenerates under the new regime. **Medium:** trading costs deduplicated into a module-level `TRADING_COSTS` shared by live and headless portfolios. **Low:** `_tSample()` reuses `_gauss()` (RNG draw order preserved — bit-identical output verified); engine feeds evaluators the full 60-candle `MAX_LOOKBACK_WINDOW` (was 50); `runBatchBacktest` filters `buy_and_hold` from tested strategies with a warning. Validation: 16/16 harness checks incl. 18/18 headless==live parity, live-baseline==headless-B&H equivalence, and seeded determinism. Full 50-seed realism+randomize re-run is **bit-identical** to the pre-fix batch (breakout mean α +8912%, median +2369%) — the clamp artifact is unchanged and still the known open issue
- **2026-07-31**: Bug-fix batch on the simulation terminal (8 items) — GARCH omega now defaults to `(1−α−β)·σ²` so custom alpha/beta without omega yield the correct long-run variance; domain randomization wired into headless/batch runs (`randomize` flag, defaults ON when realism is ON, same-seed baseline & strategy share identical randomized params); `reset()` epoch fixed via shared `_computeEpochStart()`; `openPosition()` rejects unaffordable fills (returns null) instead of silently downsizing; buy-and-hold baseline now incremental O(1) mark-to-market (no re-simulation); batch report adds p10/p25/p75/p90 percentiles + randomization header; `strategies.js` exports `calcHighest`/`calcLowest`; `formatBatchReport()` guards empty strategy lists. Validation harness: 13/13 checks pass incl. 18/18 headless==live parity (return/equity/trades/DD) and live-incremental-baseline == headless-B&H equivalence. **Re-run (realism ON + randomize ON, 50 seeds × 20k ticks):** regimes now vary per seed (B&H baseline across seeds: mean +78.5%, median +2.4%, min −14.7%, max +984%, σ 192%) but the realism-mode clamp artifact persists — breakout median α +2369% (mean +8912%, σ 19082%), RSI median α +2597%, both beat B&H in 98% of seeds. Percentiles confirm the skew: breakout p10 +339% / p90 +16399%. **Conclusion unchanged:** realism-mode alpha remains an artifact of the price clamp, not edge; legacy-mode numbers are the only credible ones (see open issue)
- **2026-07-31**: Simulation terminal (`simulation.html`) — buy-and-hold baseline strategy, same-seed baseline-relative stats (Return vs B&H), confidence-scaled position sizing (5–15% of cash), seeded PRNG (mulberry32), headless `runHeadless()` mode, multi-seed batch backtester (`js/backtest.js`, 50 seeds × 20k ticks), Node-exports for CI validation. Batch framework verified deterministic (18/18 parity checks). **Finding:** in default (legacy) mode neither RSI reversion (−24% α) nor breakout (−54% α) beats buy-and-hold net of costs; realism-mode results are invalidated by a price-clamp artifact (price pins at floor/ceiling in GARCH mode) — see open issue below
- **2026-07-27**: Bug fixes — removed duplicate boot-time strategy load, fixed alt-timeframe fallback filtering, fixed position netting quantity sizing, fixed cost basis on partial reduction, replaced floating-point equality with epsilon comparison
- **2026-07-27**: Governance documentation layer established — AGENTS.md, CLAUDE.md, PROJECT_STATUS.md, ROADMAP.md, .ai/ directory, crypto-etl/docs/ directory
- **2026-07-26**: VIBE_TRADING_REFERENCE.md created — reference summary of vibe-trading patterns
